import { Identifiers } from '@angular/compiler';

export interface DiscountInterface {
    disId?: number,
    disName: string,
    disPercent: string,
    disCreatedOn: string,
    disCreatedBy: string,
    disModifiedOn: string,
    disModifiedBy: string,
    disDeleted: number
} 