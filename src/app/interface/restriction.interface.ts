import { Identifiers } from '@angular/compiler';

export interface RestrictionInterface {
    resId?: number,
    resName: string,
    resIcon: boolean,
    resCreatedOn: string,
    resCreatedBy: string,
    resModifiedOn: string,
    resModifiedBy: string,
    resDeleted: number
} 